<Window x:Class="POS.Wpf.Views.LabelPrintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:POS.Wpf.Views"
        Title="In mã vạch và tem giá" Width="1180" Height="760"
        MinWidth="900" MinHeight="600" WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}"
        PreviewKeyDown="OnPreviewKeyDown"
        Closing="OnWindowClosing">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="In mã vạch và tem giá" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock Text="Kiểm tra nội dung, kích thước và số lượng trước khi in." Style="{StaticResource SecondaryTextStyle}" Margin="0,5,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,0,24" Margin="0,0,18,0">
                <StackPanel>
                    <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="MẪU TEM VÀ KÍCH THƯỚC" Style="{StaticResource CaptionTextStyle}" />
                            <TextBlock Text="Mẫu có sẵn" Margin="0,10,0,3" />
                            <ComboBox x:Name="TemplateComboBox" ItemsSource="{Binding TemplateOptions}" SelectedItem="{Binding SelectedTemplateOption, Mode=TwoWay}" DisplayMemberPath="DisplayName" Height="36" AutomationProperties.Name="Mẫu có sẵn" />
                            <Grid Margin="0,12,0,0">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <StackPanel><TextBlock Text="Rộng (mm)" /><TextBox Text="{Binding WidthText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsCustomTemplate}" Margin="0,3,6,0" Height="34" /></StackPanel>
                                <StackPanel Grid.Column="1"><TextBlock Text="Cao (mm)" /><TextBox Text="{Binding HeightText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsCustomTemplate}" Margin="6,3,0,0" Height="34" /></StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="MÁY IN TEM" Style="{StaticResource CaptionTextStyle}" />
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <ComboBox ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter, Mode=TwoWay}" DisplayMemberPath="Name" Height="36" />
                                <Button x:Name="RefreshPrintersButton" Grid.Column="1" Content="↻" Command="{Binding RefreshPrintersCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Width="38" Height="36" ToolTip="Tải lại danh sách máy in" AutomationProperties.Name="Tải lại danh sách máy in" />
                            </Grid>
                            <TextBlock Text="{Binding PrinterWarning}" Foreground="{StaticResource WarningBrush}" TextWrapping="Wrap" Margin="0,8,0,0" />
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="SẢN PHẨM VÀ SỐ LƯỢNG" Style="{StaticResource CaptionTextStyle}" />
                            <ItemsControl ItemsSource="{Binding Products}" Margin="0,8,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,7">
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="62" /></Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding ProductName}" />
                                                    <TextBlock Text="{Binding ProductCode}" Foreground="{StaticResource GoldBrush}" FontSize="11" />
                                                    <TextBlock Text="{Binding Barcode, TargetNullValue=Chưa có Barcode}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" />
                                                    <TextBlock Text="{Binding ErrorText}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" FontSize="10" Margin="0,4,6,0" />
                                                </StackPanel>
                                                <TextBox Grid.Column="1" Text="{Binding QuantityText, UpdateSourceTrigger=PropertyChanged}" Height="30" VerticalAlignment="Top" AutomationProperties.Name="Số lượng tem" ToolTip="Nhập số lượng từ 1 đến 1.000." />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock x:Name="ValidationSummaryText" Text="{Binding ValidationSummary}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding TotalLabelsText}" FontWeight="SemiBold" Foreground="{StaticResource GoldBrush}" Margin="0,10,0,0" />
                        </StackPanel>
                    </Border>

                    <Expander x:Name="AlignmentExpander" Header="Căn chỉnh máy in" IsExpanded="False" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Dùng khi tem in thực tế bị lệch so với vị trí mong muốn." Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" Margin="0,0,0,10" />
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <StackPanel><TextBlock Text="Dịch ngang X (mm)" /><TextBox Text="{Binding OffsetXText, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,6,0" Height="34" /></StackPanel>
                                <StackPanel Grid.Column="1"><TextBlock Text="Dịch dọc Y (mm)" /><TextBox Text="{Binding OffsetYText, UpdateSourceTrigger=PropertyChanged}" Margin="6,3,0,0" Height="34" /></StackPanel>
                            </Grid>
                            <TextBlock Text="Lề trong (mm)" Margin="0,10,0,0" />
                            <TextBox Text="{Binding InnerMarginText, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,0,0" Height="34" />
                            <Button x:Name="TestPrintButton" Content="In thử 1 tem" Command="{Binding TestPrintCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,12,0,0" HorizontalAlignment="Left" ToolTip="Gửi một tem kiểm tra rồi giữ cửa sổ để tiếp tục căn chỉnh." />
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="18">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <TextBlock Text="XEM TRƯỚC" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock x:Name="PreviewStatusText" Grid.Column="1" Text="{Binding PreviewMessage}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Margin="16,0,0,0" />
                    </Grid>
                    <Border Grid.Row="1" Background="#FFF8F0E7" Margin="0,12" ClipToBounds="True">
                        <Grid>
                            <Viewbox Stretch="Uniform" Margin="22" Visibility="{Binding IsPreviewValid, Converter={StaticResource BooleanToVisibilityConverter}}"><local:LabelPreviewHost x:Name="PreviewHost" /></Viewbox>
                            <TextBlock x:Name="PreviewEmptyStateText" Text="Nhập thông tin hợp lệ để xem tem." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsPreviewValid, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                        </Grid>
                    </Border>
                    <StackPanel x:Name="PreviewPaginationPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="{Binding IsPreviewValid, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button Content="‹" Command="{Binding PreviousPreviewCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="42" />
                        <TextBlock Text="{Binding PreviewPageNumber}" Margin="14,0" VerticalAlignment="Center" FontWeight="SemiBold" />
                        <TextBlock Text=" / " VerticalAlignment="Center" />
                        <TextBlock Text="{Binding PreviewPageCount}" VerticalAlignment="Center" Margin="2,0,14,0" />
                        <Button Content="›" Command="{Binding NextPreviewCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="42" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center" MinHeight="24" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="CloseButton" Content="Đóng" Command="{Binding CloseCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" IsCancel="True" MinWidth="82" />
                <Button x:Name="PrintButton" Content="{Binding PrintButtonText}" Command="{Binding PrintCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0" MinWidth="130" ToolTip="{Binding CanPrintReason}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
